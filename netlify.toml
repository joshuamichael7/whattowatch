# Netlify configuration file

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Edge functions configuration
[[edge_functions]]
  function = "omdb-edge"
  path = "/.netlify/edge-functions/omdb-edge"

# Redirects and rewrites
[[redirects]]
  from = "/api/omdb-edge"
  to = "/.netlify/edge-functions/omdb-edge"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
